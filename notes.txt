

Three event interfaces:

1. from low-level IO from app/system to GUI
     mouse/keyboard events
2. from GUI to widgets
     
3. from GUI to userspace


If we want loadable forms, we can't transmit (load) meaningful pointers.  We can resolve an index/relative at load-time.
